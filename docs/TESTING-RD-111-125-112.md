# How to test RD-111 / RD-125 / RD-112

**Tickets:** RD-111 (submission stage buttons) · RD-125 (interview rounds UI) · RD-112 (job kanban)  
**Owner:** Dev B · Day 3  
**Automated:** `cd server && npm test` — includes `submissions-pipeline-ui.test.js` (**77** tests total).

## Prerequisites

```powershell
docker compose up -d --build
docker compose exec server node prisma/seed.js
```

Need: active requirement with open seat + candidate + at least one submission (use RD-108 put-forward as Recruiter).

## Automated

```powershell
cd server
npm test
npx jest --runInBand tests/submissions-pipeline-ui.test.js tests/submissions-stage.test.js
```

## Manual

### RD-111 — Stage buttons

1. Recruiter → open a submission detail.  
2. Under **Stage**, click **Move to internal screening** → confirm.  
3. Try **Move to backout** without reason → Confirm disabled; enter reason → succeeds and locks path.  
4. Advance toward offer: add/resolve interview rounds first (RD-125) or API will reject offer if rounds pending.

### RD-125 — Interview rounds

1. On submission detail → **+ Add round**.  
2. Type **Internal**, result **pass**, feedback + rating → Save.  
3. Add **Client L1** with schedule + pending.  
4. **Edit** a round → change feedback/result → Save.  
5. List refreshes with badges.

### RD-112 — Kanban

1. Requirement detail → **Pipeline board** (or `/requirements/:id/board`).  
2. Columns = pipeline stages + backout/rejected.  
3. Cards show candidate; click opens submission.  
4. Recruiter can use **→ next stage** chips on cards (prompt for backout/reject reason).

## Done checklist

- [ ] Stage buttons + reason modals on `/submissions/:id`  
- [ ] Interview add/edit modal (internal + client)  
- [ ] Board at `/requirements/:id/board`  
- [ ] `npm test` green  
